import { CheckCartExistsInlocal } from './check-cart-exists-inlocal';

describe('CheckCartExistsInlocal', () => {
  it('should create an instance', () => {
    expect(new CheckCartExistsInlocal()).toBeTruthy();
  });
});
