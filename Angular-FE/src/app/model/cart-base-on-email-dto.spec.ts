import { CartBaseOnEmailDto } from './cart-base-on-email-dto';

describe('CartBaseOnEmailDto', () => {
  it('should create an instance', () => {
    expect(new CartBaseOnEmailDto()).toBeTruthy();
  });
});
