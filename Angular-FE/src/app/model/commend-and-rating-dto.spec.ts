import { CommendAndRatingDTO } from './commend-and-rating-dto';

describe('CommendAndRatingDTO', () => {
  it('should create an instance', () => {
    expect(new CommendAndRatingDTO()).toBeTruthy();
  });
});
